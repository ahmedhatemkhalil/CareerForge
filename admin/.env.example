# =============================================================================
# CareerForge Admin — Railway environment variables
# Embedded at BUILD time — set in Railway before deploy/rebuild
# Railway → your admin service → Variables
# =============================================================================

# Backend API base URL (required)
# Must include /api — use your deployed backend Railway URL
API_URL=https://your-backend.up.railway.app/api
